<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginTop="20dp"
    android:padding="8dp">

    <!-- TextView với margin bottom để tạo khoảng cách với RecyclerView -->
    <TextView
        android:id="@+id/txt_manage_products"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="16dp"
        android:text="QUẢN LÝ SẢN PHẨM"
        android:textAlignment="center"/>

    <!-- RecyclerView hiển thị danh sách sản phẩm -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_product_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/txt_manage_products"
        android:layout_marginBottom="70dp" />

    <!-- FloatingActionButton để thêm sản phẩm -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_product"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="16dp"
        android:backgroundTint="@color/or"
        app:srcCompat="@drawable/ic_add"
        app:tint="@android:color/white" />
</RelativeLayout>
